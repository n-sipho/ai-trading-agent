rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /trading_logs/{document=**} {
      allow read: if true;
      allow write: if false; 
    }
    match /bot_commands/{document=**} {
      allow read, write: if true;
    }
  }
}
